import { useRef } from 'react'
import { motion } from 'framer-motion'
import { ArrowDown, Circle, Sparkles } from 'lucide-react'
import DoorAnimation from './DoorAnimation'
export default function Hero({t}){const ref=useRef(null);const move=e=>{const r=ref.current?.getBoundingClientRect();if(r)ref.current.style.setProperty('--mx',`${(e.clientX-r.left-r.width/2)/45}px`)};return <header id="legend" className="hero shell hero-split" ref={ref} onMouseMove={move} onMouseLeave={()=>ref.current?.style.setProperty('--mx','0px')}><div className="particles" aria-hidden="true">{Array.from({length:10},(_,i)=><i key={i}/>)}</div><div className="hero-copy"><div className="status"><Circle size={9} fill="currentColor"/>{t.status}</div><motion.p className="eyebrow" initial={{opacity:0}} animate={{opacity:1}}>{t.eyebrow}</motion.p><motion.h1 initial={{opacity:0,y:24}} animate={{opacity:1,y:0}} transition={{duration:.75}}>{t.hero}</motion.h1><p className="meaning"><Sparkles size={15}/>{t.meaning}</p><p className="hero-sub">{t.sub}</p><div className="hero-actions"><a className="btn primary" href="#packages">{t.call}</a><a className="btn ghost" href="#origin">{t.lore}<ArrowDown size={16}/></a></div></div><DoorAnimation t={t}/></header>}
