import { motion } from 'framer-motion'
import { BadgeCheck, Coffee, Contact, DoorOpen, KeyRound, ShieldCheck } from 'lucide-react'
import { featureTranslations } from '../data/featureTranslations'
const icons=[Contact,DoorOpen,ShieldCheck,Coffee]
export default function ProfileSection({lang}){const c=featureTranslations[lang]||featureTranslations.en;return <section className="section shell profile-section" id="profile"><div className="section-kicker">{c.profileKicker}</div><div className="profile-card glass"><div className="profile-visual"><motion.div className="profile-placeholder" animate={{boxShadow:['0 0 0 0 rgba(247,223,99,.18)','0 0 0 18px rgba(247,223,99,0)']}} transition={{duration:2.4,repeat:Infinity}}><span>U</span></motion.div><div className="profile-status"><i/> {c.online}</div></div><div className="profile-copy"><span className="profile-label">{c.founder}</span><h2>Umut <BadgeCheck/></h2><p>{c.status}</p><div className="profile-skills">{c.skills.map((label,i)=>{const Icon=icons[i];return <motion.span key={label} initial={{opacity:0,scale:.9}} whileInView={{opacity:1,scale:1}} transition={{delay:i*.08}} viewport={{once:true}}><Icon/>{label}</motion.span>})}</div><div className="profile-signature"><KeyRound/> {c.signature}</div></div></div></section>}
